#include <bits/stdc++.h>

using namespace std;

int main(){
    string a, b;
    getline(cin, a);
    getline(cin, b);
    int lengthA, lengthB, counter = 0, distance = 0;
    lengthA = a.size();
    lengthB = b.size();
    while(counter <= lengthA){
     if (a[counter] == b[counter]){
         counter++;
         continue;
     }else{
     }
    }
    return 0;
}