#include <bits/stdc++.h>

using namespace std;

int main(){
    int T;
    cin >> T;
    while(T--){
        long long a, b, s;
        string strA, strB = "", strS;
        cin >> a >> s;
        strA = to_string(a);
        strS = to_string(s);
        for (int j = strS.length()-1; j >= 0; j++){
            

        }

    }
    return 0;
}

