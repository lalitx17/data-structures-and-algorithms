#include <iostream>
#include <algorithm>

using namespace std;

int main(){
    int t = 0;
    cin >> t;
    for (int i = 1; i <= t; i++){
        int n = 0,p = 0;
        int dice[1000001];
        cin >> n;
        for (int j = 0; j < n; j++){
            cin >> dice[j];
        }
        sort(dice, dice+n);
        for (int k = 0; k <= n-p;k++){
            if (k > )
        }


    }
    return 0;
}